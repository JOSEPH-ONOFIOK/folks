node_modules
.next
*.tsbuildinfo
next-env.d.ts

# Raw allowlist exports. data/folklist.json is generated from these and is
# what the app reads; the CSVs stay local to avoid committing them twice.
eligible/

# Contract build output
.hh-cache/
.hh-artifacts/
.hh-artifacts-folks.json

# Local env
.env*.local
.env.deploy
deployed.json

# wallet test build
.wallet-test/
